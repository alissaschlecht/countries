const testFolder = './images/';
const fs = require('fs');

console.log(testFolder);
console.log(fs);
// fs.readdir(testFolder, (err, files) => {
//   files.forEach(file => {
//     console.log(file);
//   });
// });